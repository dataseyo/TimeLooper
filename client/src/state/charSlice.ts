import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const loopSlice = createSlice({
    name: "loop",
    initialState,
    reducers: {

    }
})

// export actions
export const {
    
} = loopSlice.actions

// export reducer
export default loopSlice.reducer